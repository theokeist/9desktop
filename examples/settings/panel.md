# Settings: Panel

Rows:
- Placement: top / bottom / left
- Modules: reorder + enable/disable

Rules:
- Placement is shell-owned; Settings writes config and asks shell to apply.
- Reorder must be keyboard-friendly (up/down buttons are acceptable).
