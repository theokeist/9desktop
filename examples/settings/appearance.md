# Settings: Appearance

Rows:
- Preset: terminal / dark / glass
- Alpha: 0..255 (only meaningful for glass)

Rules:
- Preset applies immediately unless the shell requires restart.
- Alpha updates preview continuously but commits on mouse-up (avoid redraw storms).
